# CRS function

in order to plot the data with the terra::plet function, the crs(x)
function should be ran locally, could be due to a bad call of CRS in the
leaflet package See terra::crs for more information about the function.

## Usage

``` r
crs(x, proj = FALSE, describe = FALSE, parse = FALSE)
```
